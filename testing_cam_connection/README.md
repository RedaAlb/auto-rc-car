# The testing_cam_connection directory

This directory is used test and analyse the camera network connection between the computer and the Pi.

- `/cam_connection`, contains files to test the camera connection and provide some insight to the connection.
    - `testing_cam_connection.py`, used to test camera connection and to obtain data about the connection, which is then used to
    plot some graphs to visualise the connection.